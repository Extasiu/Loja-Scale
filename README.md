# Achadinhos da Bruna

Página promocional standalone (React + Vite + Tailwind v4).

## Como rodar

```bash
npm install
npm run build
```

Para desenvolvimento local:

```bash
npm run dev
```

Os arquivos finais ficam em `dist/`.
